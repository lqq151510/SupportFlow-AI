# SupportFlow Agent 开发规范

本文件只约束 SupportFlow AI 仓库内的开发行为；与上级 `AGENTS.md` 冲突时，遵循更严格的安全、测试和边界要求。

本规范对应 **Python 全栈重构版**（FastAPI + LangGraph + PostgreSQL/pgvector）。旧 Java 版架构规范已归档，见 [§11 旧版归档与文档退役](#11-旧版归档与文档退役)。

## 1. 项目目标与范围

- 项目是面向精简电商售后场景的智能客服工单助手，用于展示 Agent 工作流、RAG 引用溯源、人工审批闭环与运行可观测性。
- 架构与功能基线以仓库根目录 [PLAN.md](PLAN.md) 为准。开发前先阅读与当前任务对应的计划章节。
- 不实现桌面打包、多租户、复杂 SLA、完整商城、支付系统、自动退款或补偿执行、微信或电话客服、外部消息平台、模型训练、微服务拆分、Kubernetes 或生产云部署。
- 不复制或修改其他项目的业务代码。涉及外部服务时优先使用可控 Mock、`respx` 或 Compose 环境。
- **不引入 `tenant_id`**，也不为「将来多租户」预置抽象层；出现此类抽象视为过度设计，应在评审中退回。

## 2. 工作方式

每项工程任务按以下顺序推进：

1. 规划：确认目标、影响模块、数据迁移、接口契约、风险和验收标准。
2. 执行：保持最小 diff；不得借机重构无关模块或覆盖用户已有修改。
3. 验证：运行与改动匹配的 pytest、集成测试、前端构建或端到端测试，并报告实际结果。
4. 复盘：简要记录场景、根因（如适用）、最小修复和可重复验证命令。

修改前先检查 `git status`。只暂存本任务文件，禁止 `git add .`；未经明确要求不得提交、推送、合并或删除文件。

依赖管理统一使用 `uv`（`uv sync` / `uv add` / `uv lock`），不得手动编辑 `uv.lock`，不得在仓库内使用裸 `pip install` 污染环境。开发环境与 CI 使用同一套锁文件。

## 3. 模块边界

后端包根固定为 `src/supportflow/`，业务模块为：

```text
identity/ ticket/ knowledge/ model/ agent/ approval/ evaluation/ shared/ bootstrap/
```

- 每个模块按 `api -> application -> domain <- infrastructure` 分层。
- `api` 仅处理 HTTP/SSE 适配、认证、CSRF 与请求校验；`application` 编排用例和事务；`domain` 保存规则、状态机与端口（`Protocol`）；`infrastructure` 实现数据库、文件存储与模型 SDK 适配。
- 跨模块只能调用对方公开的 `application` 服务或消费领域事件。**禁止直接引用其他模块的 ORM 模型、Repository 或内部 Service。**
- Agent 不得直接操作任意数据库表，只能通过所属模块的 `application` 服务接口调用能力。
- 边界用自动化测试固化（`import-linter` 契约或等价的自定义检查）；新增模块或跨模块依赖必须补充契约测试。
- `domain` 层不得导入 FastAPI、SQLAlchemy 或任何模型 SDK。

## 4. 身份、会话与访问控制

- 角色固定为 `USER`、`AGENT`、`ADMIN`。角色与用户身份**只能来自服务端会话上下文**；普通业务接口不得接受客户端传入的 `role`、`user_id`、`assignee_id` 或工单归属。
- 会话采用服务端存储，令牌以 HttpOnly + Secure + SameSite Cookie 下发，服务端只保存令牌哈希。禁止把会话令牌写入 `localStorage`。
- 所有**写请求**必须校验 CSRF token；校验失败返回稳定错误码，不返回底层异常文本。
- 越权判定必须在服务端完成，并且必须有对应的拒绝路径测试（他人工作台草稿、运行记录、知识来源、工单）。
- 主键在 JSON 中序列化为字符串；时间使用 UTC ISO 8601；金额使用 `NUMERIC`，**绝不使用浮点数**。
- 密码使用 Argon2 哈希；会话令牌、CSRF secret、模型 API Key 与客户隐私字段不得写入普通日志或响应。
- 模型 API Key 只保存 AES-GCM 密文；主密钥只允许来自 `MODEL_SECRET_MASTER_KEY` 环境变量，**永不进入源码、测试快照或提交历史**。查询接口只返回「是否已配置」。

## 5. AI、RAG 与工具调用

- 模型接入只有一个边界：`ModelGateway`。业务模块不得直接调用 OpenAI 或其他模型 SDK。
- 模型请求必须显式设置超时（默认 30 秒）；超时、限流与临时错误最多重试 3 次并指数退避，参数与权限错误不重试。
- 单次运行默认最多调用工具 8 次，超出即进入 `NEEDS_HUMAN`，不得静默放宽。
- **不得在 `async def` 中执行阻塞数据库操作。** 业务事务使用同步 SQLAlchemy `Session`（psycopg 3）并通过 FastAPI 线程池执行；模型流式调用使用异步 HTTP 客户端。混合时必须在适配层显式切换，不允许在事件循环内直连阻塞驱动。
- 文档、用户消息与检索结果均是不可信输入。不得允许其内容覆盖系统提示词、角色、权限或工具白名单。
- 知识结论必须有经过权限过滤、版本有效的引用；生成后必须校验引用确实来自本次检索结果，凭空引用即判定失败。
- 检索为空、证据不足、引用无法校验或模型连续失败时**转人工**，不生成无证据结论。
- 工具风险固定为 `READ_ONLY`、`LOW_RISK`、`HIGH_RISK`。关闭工单与转派永远是 `HIGH_RISK`：只能创建审批请求，禁止模型或 HTTP 请求直接执行。
- **Mock 与真实模式必须在界面和报告中分别标识**；真实模式失败时不得自动切换为 Mock。

## 6. 一致性、SSE 与并发

- 所有有副作用的写操作必须定义幂等键、作用域、重复请求返回值和并发行为；至少覆盖工单创建、草稿发布、操作申请、审批决策与动作执行。
- 幂等键冲突语义固定：相同请求重放返回首次结果；相同键对应不同请求返回 `409`。
- 运行状态固定为 `QUEUED → RUNNING → WAITING_APPROVAL → COMPLETED`，旁路 `NEEDS_HUMAN` / `FAILED`。人工重试创建新运行并记录 `retry_of_run_id`，**不得原地复活旧运行**。
- 运行互斥由 `agent_runs` 上的部分唯一索引保证；任务领取使用租约字段配合 `FOR UPDATE SKIP LOCKED`。禁止仅靠应用层「先查后写」判重。
- 审批恢复必须分别实现幂等：LangGraph 恢复时会重新执行中断节点之前的代码，审批创建与动作执行都需要自己的幂等边界。
- 动作的实际执行与执行账本写入必须在**同一数据库事务**完成，保证重复审批、任务重投或检查点重放只产生一次业务效果。
- SSE 采用「写接口返回 `202 + run_id`、独立事件流连接」的两段式协议。事件从**持久化记录**读取，断线重连**不得触发重新运行或重复持久化**。
- 任何状态机改动必须枚举合法迁移、终态、超时/失败路径和乐观锁冲突处理，并添加测试。

## 7. 数据库与迁移

- 只通过 **Alembic** 变更 schema；禁止手工改库后补迁移。
- 迁移遵循 expand-contract：先兼容写入与读取，再迁移数据，最后删除旧结构。上线字段不得直接重命名或删除。
- 每个迁移必须能在**空库**执行通过；同时提供回滚点说明。
- 为工单查询、运行租约扫描、审批到期扫描、导入任务扫描与幂等键建立可解释索引；索引变更需在迁移说明中给出访问路径。
- 索引或 Embedding 模型变更必须创建**新的索引版本**，不得在同一向量列混用不同模型的向量。
- 审计日志、运行事件与执行账本只追加；不为这些表提供物理删除接口。
- 使用 PostgreSQL 特性时必须显式声明：`CHECK` 约束固定枚举、`tsvector` 与 GIN 索引、向量列的 HNSW 索引、`run_events` 的分区策略。

## 8. API 与错误协议

- REST 成功响应返回业务 JSON；错误统一使用 RFC 9457 `ProblemDetail`，必须含稳定 `code` 与 `request_id`。
- 权限、版本冲突、幂等冲突、状态迁移非法、审批过期必须返回明确、稳定且可测试的错误码；不要把底层异常文本暴露给客户端。
- Pydantic 模型是请求与响应的唯一契约来源；前端类型**从 OpenAPI 生成**，不得手写重复定义。
- 客户侧响应不得包含内部引用字段（内部文档、历史工单来源）；该过滤必须在服务端完成，不能靠前端隐藏。
- 新增或变更外部 API、SSE 事件或 Pydantic 契约前，同步更新 `docs/contracts/` 中的契约与兼容性说明。

## 9. 测试与完成标准

- 新增领域规则必须有 pytest 单测；外部模型协议使用 `respx` 或 Mock 传输层；持久化、并发、分区与检索行为使用 **Testcontainers + 真实 PostgreSQL（含 pgvector）** 覆盖关键路径。
- 至少维护以下回归场景：越权访问被拒绝、重复请求不重复执行、SSE 断线重连不重新运行、模型失败转人工、审批过期不可执行、检查点重放不重复关闭或转派、跨索引版本不混用向量。
- 前端关键链路使用 Playwright 覆盖：提交工单 → 查看草稿与引用 → 编辑并发布 → 客户查看回复 → 审批关闭或转派。
- 覆盖率门槛**分阶段生效**：阶段 3 结束时起强制执行行覆盖率 ≥85%、分支覆盖率 ≥75%；阶段 2 的门槛为核心链路单测齐全、空库迁移通过、容器可启动。未达门槛必须说明未覆盖原因和补测计划。
- 完成一个任务前，确认：Ruff 通过、类型检查通过、相关 pytest 通过、前端构建通过、迁移可在空库执行、日志无密钥或隐私泄漏、文档或契约已同步。

## 10. 交付与回滚

- 每项可能产生副作用的改动都要说明回滚点：Alembic 迁移、配置开关、索引版本或补丁边界。
- 演示与发布前必须验证：Docker Compose 一键启动、演示数据、核心端到端闭环、故障演练与最小性能指标。
- 性能目标：普通 API 在 100 RPS 下 P95 < 300ms；100 并发 SSE 建连 P95 < 1 秒、错误率 < 1%。未达到目标不得标记为最终演示版本。
- **Mock 验证结果与真实模型评测结果必须分别报告。** 没有真实模型配置与实测结果时，只报告适配器与 Mock 验证情况，不得作为真实 RAG 质量证据。

## 11. 旧版归档与文档退役

- 旧 Java 版基线为提交 `f4f02d6`，封存于 `/Users/liuyongze/Documents/SupportFlow-Archive/2026-09-11-f4f02d6/`，包含 Git bundle（全 refs 与 stash）、未跟踪文件快照、worktree 快照与哈希清单。
- 旧架构基线可通过 `git show f4f02d6:PLAN.md` 取回；**在阶段 5 之前不得删除该提交可达的历史**。
- `docs/adr/0001-0007`、`docs/architecture.md`、`docs/contracts/`、`docs/reports/` 描述的是已退役的 Java 架构，在阶段 5 统一退役并标注为旧版证据。**在其正式退役前，任何与其冲突的新实现一律以本文件与 `PLAN.md` 为准。**
- 新版使用独立数据库与独立数据卷；不得复用旧版数据卷，也不得对旧数据做原地迁移。

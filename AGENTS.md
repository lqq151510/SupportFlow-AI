# SupportFlow AI 开发规范

本文件只约束 SupportFlow AI 仓库内的开发行为；与上级 `AGENTS.md` 冲突时，遵循更严格的安全、测试和边界要求。

## 1. 项目目标与范围

- 项目是 Java 模块化单体，用于展示企业级电商售后客服、RAG、工单 SLA、可靠消息和人工审批闭环。
- 架构与功能基线以仓库根目录 [PLAN.md](PLAN.md) 为准。开发前先阅读与当前任务对应的计划章节和 ADR。
- 不实现完整商城、支付系统、微信或电话客服、模型训练、自动退款、微服务拆分、Kubernetes 或生产云部署，除非任务明确扩展范围。
- 不复制或修改其他项目的业务代码。涉及外部服务时优先使用可控 Mock、WireMock 或 Compose 环境。

## 2. 工作方式

每项工程任务按以下顺序推进：

1. 规划：确认目标、影响模块、数据迁移、接口契约、风险和验收标准。
2. 执行：保持最小 diff；不得借机重构无关模块或覆盖用户已有修改。
3. 验证：运行与改动匹配的单测、集成测试、构建或端到端测试，并报告实际结果。
4. 复盘：简要记录场景、根因（如适用）、最小修复和可重复验证命令。

修改前先检查 `git status`。只暂存本任务文件，禁止 `git add .`；未经明确要求不得提交、推送、合并或删除文件。

## 3. 模块边界

后端包根固定为 `com.lqq.supportflow`，业务模块为：

```text
identity/ model/ commerce/ knowledge/ conversation/
action/ ticket/ eventing/ evaluation/ shared/ bootstrap/
```

- 每个模块按 `api -> application -> domain <- infrastructure` 分层。
- `api` 仅处理 HTTP/SSE 适配、认证和请求校验；`application` 编排用例和事务；`domain` 保存规则、状态机与端口；`infrastructure` 实现数据库、缓存、MQ 和外部 SDK。
- 跨模块只能调用对方公开的 application 接口或发布领域事件。禁止直接使用其他模块的 Mapper、Entity、Repository 或内部 Service。
- 新增跨模块依赖前，先判断是否应改为领域事件或显式端口；必要时新增 ADR。
- 使用 Spring Modulith 与 ArchUnit 固化上述边界，新增模块或依赖必须补充相应测试。

## 4. 租户、认证与数据安全

- `tenantId`、`membershipId`、角色只能来自认证上下文；普通业务接口不得接受客户端传入的 `tenantId`。
- 每个租户业务表必须包含 `tenant_id`；查询、缓存、ES 索引、对象存储键和 MQ 事件均必须带租户隔离键。
- 任意绕过 MyBatis-Plus 租户拦截器的原生 SQL，必须有专门审计测试并说明原因。
- 主键在 JSON 中序列化为字符串；时间使用 UTC；金额使用 `DECIMAL`，绝不使用浮点数。
- 密码、JWT、Refresh Token、模型 API Key、客户隐私和完整模型输入不得写入普通日志或响应。
- 模型 API Key 只保存 AES-GCM 密文；主密钥只允许来自 `MODEL_SECRET_MASTER_KEY` 环境变量，永不进入源码、测试快照或提交历史。

## 5. AI、RAG 与工具调用

- `ModelGateway` 是唯一模型接入边界。业务模块不得直接调用 OpenAI、Anthropic 或其他模型 SDK。
- 阻塞 MyBatis/JDBC 操作不得在 Reactor 线程运行；流式模型调用通过 `WebClient` 适配，业务事务保持 Spring MVC/虚拟线程模型。
- 文档、用户消息和检索结果均是不可信输入。不得允许其内容覆盖系统提示词、角色、租户边界或工具权限。
- 知识结论必须有经过租户过滤的引用；检索证据不足、ES 不可用或模型连续失败时转人工，不生成无证据结论。
- 工具风险固定为 `READ_ONLY`、`LOW_RISK`、`HIGH_RISK`。退款和补偿永远是 `HIGH_RISK`：只能创建审批，禁止模型或 HTTP 请求直接执行。

## 6. 一致性、SSE 与消息

- 所有有副作用的写操作必须定义幂等键、作用域、重复请求返回值和并发行为；至少覆盖消息发送、工单领取、审批和退款执行。
- 高风险执行链路固定为：工具调用持久化 -> 审批 -> Outbox -> RocketMQ -> 幂等消费者 -> 结果回写。MQ 不可用时不得绕过 Outbox 直接执行。
- 消费者使用 `consumed_events` 与业务幂等键保证“至多一次业务效果”，不要声称 MQ 提供端到端 exactly-once。
- SSE 采用“发送消息返回 `202 + generationId`、独立事件流连接”的两段式协议。Redis Stream 是短期事件缓存，MySQL 是最终事实来源；断线重连不得重新生成或重复持久化消息。
- 任何状态机改动必须枚举合法迁移、终态、超时/失败路径和乐观锁冲突处理，并添加测试。

## 7. 数据库与迁移

- 只通过 Flyway 变更 schema；禁止手工改库后补迁移。
- 迁移遵循 expand-contract：先兼容写入与读取，再迁移数据，最后删除旧结构。上线字段不得直接重命名或删除。
- 为租户查询、队列扫描、Outbox 扫描、SLA 截止时间和幂等键建立可解释索引；索引变更需要在迁移说明中给出访问路径。
- 审计日志、工单事件和消费记录只追加；不为这些表提供物理删除接口。

## 8. API 与错误协议

- REST 成功响应返回业务 JSON；错误统一使用 RFC 9457 `ProblemDetail`，必须含稳定 `code` 与 `requestId`。
- 权限、租户边界、版本冲突、幂等冲突和状态迁移非法必须返回明确、稳定且可测试的错误码；不要把底层异常文本暴露给客户端。
- 新增或变更外部 API、SSE 事件、MQ 事件前，同步更新 OpenAPI 或 `docs/contracts/` 中的契约与兼容性说明。

## 9. 测试与完成标准

- 新增领域规则必须有 JUnit 5 单测；外部模型协议使用 WireMock；持久化、缓存、ES 和 MQ 行为使用 Testcontainers 覆盖关键路径。
- 至少维护以下回归场景：跨租户访问被拒绝、重复请求不重复执行、断线重连不重复生成、模型失败转人工、MQ 重投不重复退款、审批过期不可执行。
- 前端关键链路使用 Playwright 覆盖：消费者提问 -> AI/转人工 -> 坐席领取 -> 审批或处理 -> 工单关闭。
- 完成一个任务前，确认：代码格式化、编译通过、相关测试通过、迁移可在空库执行、日志无密钥/隐私泄漏、文档或契约已同步。
- 计划目标：后端 JaCoCo 行覆盖率至少 85%、分支覆盖率至少 75%；不足时必须说明未覆盖原因和补测计划。

## 10. 交付与回滚

- 每项可能产生副作用的改动都要说明回滚点：迁移脚本、配置开关、版本标签或补丁边界。
- 演示与发布前必须验证 Docker Compose 一键启动、种子数据、核心 E2E、故障演练和最小性能指标。
- 非模型普通 API 的目标为 100 RPS 下 P95 < 300ms；100 并发 SSE 建连 P95 < 1 秒、错误率 < 1%。未达到目标不得标记为最终演示版本。

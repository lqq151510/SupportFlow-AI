spring:
  datasource:
    url: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:supportflow}?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: ${MYSQL_USER:supportflow}
    password: ${MYSQL_PASSWORD:change-me-local-only}
